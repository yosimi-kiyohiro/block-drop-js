# AGENTS.md — Block Drop（JavaScript版テトリス風パズル）

AI アシスタントがこのプロジェクトを触る時に最初に読むファイル。

## このプロジェクトについて

HTML / CSS / JavaScript（フレームワークなし・ビルドなし）で作った落ちものブロックパズル。
プログラミング学習プロジェクトで、**コードのわかりやすさを最優先**する。
Phase 2 で C言語版を作り、Phase 3 で両者を比較学習する計画（進捗は Obsidian の permanent-note「テトリスゲームプロジェクト」参照）。

## ファイル構成

| ファイル | 役割 |
|---|---|
| index.html | 画面レイアウト・Canvas・タッチボタン・オーバーレイ |
| style.css | デザイン・スマホ縦持ち対応（PC は盤面拡大表示） |
| game.js | ゲームロジック全体（1ファイル。セクションコメントで区切る） |

## 設計ルール（変更時も必ず守ること）

1. **状態は gameState オブジェクトに一元管理**する。バラのグローバル変数を増やさない
2. **ゲームの時間管理は requestAnimationFrame に一本化**する。ロック遅延・演出などに setTimeout を使わない（経過時間 elapsed を delta で加算する方式）
   - 例外：タッチボタン長押しの setInterval のみ可。ただし必ず `repeatStoppers` に停止関数を登録する
3. **入力の可否判定は `canControl()` を通す**（開始前・一時停止・ゲームオーバー・演出中は入力無視）
4. **描画は論理座標（盤面 300×600・セル30px）のまま書く**。表示サイズ・devicePixelRatio の違いは `setupCanvas()` が吸収する
5. シャッフルは **Fisher-Yates**（`sort(() => Math.random() - 0.5)` は偏るので禁止）
6. リスタート処理は `resetGame()` に集約（タイマー停止→状態初期化の後始末一覧）
7. コメントは日本語。学習用なので「なぜそうするか」を書く
8. game.js が **500行を超えたら**ファイル分割をオーナーに相談する

## 動作確認の方法

- **画面確認**：index.html をブラウザで開く（変更後は Ctrl+F5。表示サイズはロード時に確定するためリサイズ後も再読み込み）
- **構文チェック**：`node --check game.js`
- **ロジックテスト**：document / requestAnimationFrame をスタブして game.js を eval する Node スクリプトで、ライン消去・スコア・ロック遅延などを検証できる（git 履歴のコミットメッセージ参照）
- **スマホ確認**：DevTools のデバイスエミュレーション（375px〜487px・縦持ち）

## 注意事項

- スマホは縦持ち専用（横持ちレイアウトは意図的に未対応）
- 公開先：GitHub Pages（master ブランチ直下を配信）。push すると数十秒〜数分で本番に反映される
- 「テトリス」は商標のため、公開物のタイトルは「Block Drop」を使う
